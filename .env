# WebSocket Configuration
# Note: WebSocket server must be running for this to work
# Development: ws://localhost:8080
# Production: wss://your-domain.com/ws
VITE_WEBSOCKET_URL=ws://localhost:8080

# API Configuration
VITE_API_URL=http://localhost:3000

# Feature Flags
# Set VITE_ENABLE_WEBSOCKET=true only when WebSocket server is running
# Otherwise, the app will use local GPS simulation
VITE_ENABLE_WEBSOCKET=false
VITE_ENABLE_LOCAL_SIMULATION=true

# Application Environment
VITE_ENVIRONMENT=development
